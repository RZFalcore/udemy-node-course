<%- include("../includes/head.ejs") %>
</head>

<body>
    <%- include("../includes/navigation.ejs") %>
<main>
    <% if (products.length > 0) { %>
        <ul>
            <% products.forEach( product => { %>
                <li>
                    <p><%= product.productData.title %>: (<%= product.productQty %>)</p>
                </li>
             <% }) %>   
        </ul>
    <% } else { %>
        <h1>Cart is empty!</h1>
    <% } %>
</main>
<%- include("../includes/end.ejs") %>